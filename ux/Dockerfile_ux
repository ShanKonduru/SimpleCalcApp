# Use a lightweight web server image to serve the UI
FROM nginx:alpine

# Copy the static files to the web server directory
COPY ux/ /usr/share/nginx/html/

# Expose port 80 for web server
EXPOSE 80

# Start the web server
CMD ["nginx", "-g", "daemon off;"]
